double global = 0;

double main() {

	double x;
	double y = 100;
	double result = 0;

	while (x + y) {
		result = sum(x, y);
	};

	return result;
};

double sum(double x, double y) {
	double result = floor(sin(global) + cos(y) - sqrt(x + y));
	return result;
};
