double global = 4 + 2 - 1;

double main() {

	double x = 10 ^ 2;
	double y = 8 - 8;
	double result = 1 * x;

	while (x + y) {
		result = sum(x, y);
	};

	return result;
};

double sum(double x, double y) {
	double result = floor(sin(global) + cos(y) - sqrt(x + y));
	return result;
};
